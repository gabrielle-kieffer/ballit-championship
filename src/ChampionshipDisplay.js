import React from 'react';

const ChampionshipDisplay = ({ teams }) => {
  // return (
  //   <div>
  //     <h2>Times Sorteados</h2>
  //     {teams.map((team, index) => (
  //       <div key={index}>{index + 1}. {team.name} - {team.slogan} ({team.year})</div>
  //     ))}
  //   </div>
  // );
};

export default ChampionshipDisplay;
